"use client"

/**
 * Temporary stub for the removed Time-Slot Selector.
 * It renders nothing but satisfies existing imports so the
 * build succeeds. Remove the imports in other files before
 * deleting this component permanently.
 */
export function TimeSlotSelector() {
  return null
}

export default TimeSlotSelector
